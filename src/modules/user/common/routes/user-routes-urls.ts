const baseRoute = "/user";

const userRoutesUrls = {
  dashboard: `${baseRoute}/dashboard/`,
  listing: `${baseRoute}/listing/`,
  details: `${baseRoute}/:id/details/`,
};

export default userRoutesUrls;
