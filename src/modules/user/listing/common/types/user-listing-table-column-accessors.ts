import { ReactNode } from "react";

type UserListingTableColumnAccessors = {
  id: ReactNode;
  name: ReactNode;
  email: ReactNode;
  createdAt: ReactNode;
  isActive: ReactNode;
};

export default UserListingTableColumnAccessors;
